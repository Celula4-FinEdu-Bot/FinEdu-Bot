{
  "name": "qa-e2e-playwright",
  "private": true,
  "version": "1.0.0",
  "description": "Pruebas E2E de QA (capa CT) para la app Blazor Server + integración MEF",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.4.1"
  }
}
